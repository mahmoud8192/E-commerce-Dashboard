import { Eye } from "lucide-react";
import Button from "../common/Button";

function Action({
  name = "View",
  onclick,
  className = "",
  icon = <Eye size={16} />
}) {
  return (
  );
}

export default Action;
